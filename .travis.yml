language: c
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get -qq update
install:
  - sudo apt-get -qq install rake bison git gperf libcgroup-dev cgmanager
  - gem install rake
  - sudo service cgmanager start
script: 
  - rake all test
